<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.superflow">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/SuperFlowTheme">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.OnboardingActivity"
            android:exported="false"
            android:label="@string/app_name" />

        <activity
            android:name=".ui.HabitDesignerActivity"
            android:exported="false"
            android:label="@string/habit_designer"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ui.HabitDetailActivity"
            android:exported="false"
            android:label="@string/habit" />

        <activity
            android:name=".ui.BlueprintActivity"
            android:exported="false"
            android:label="@string/blueprint_studio"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ui.AiEngineActivity"
            android:exported="false"
            android:label="@string/ai_engine" />

        <activity
            android:name=".ui.ActivityLogActivity"
            android:exported="false"
            android:label="@string/activity_trail" />

        <activity
            android:name=".ui.ReviewActivity"
            android:exported="false"
            android:label="@string/review" />

        <activity
            android:name=".ui.ScorecardActivity"
            android:exported="false"
            android:label="@string/habit_scorecard" />

        <activity
            android:name=".ui.FlowActivity"
            android:exported="false"
            android:label="@string/flows" />

        <activity
            android:name=".ui.RecoveryActivity"
            android:exported="false"
            android:label="@string/recovery_center" />

        <receiver
            android:name=".notify.ReminderReceiver"
            android:exported="false" />

        <receiver
            android:name=".notify.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
