<?xml version="1.0" encoding="utf-8"?>
<!--
  Dark-flavour widget backgrounds.

  RemoteViews cannot be handed a Drawable, only a resource id, so each dark
  flavour needs its own shape declared up front. Colours are the literals
  from Ramps.flatDay ("sf_dark_*_surface"/"outline_variant"); they cannot
  be @color references because the launcher resolves them, and a night
  qualifier would follow the launcher's mode rather than the app's.
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="#FF1C1B17" />
    <corners android:radius="24dp" />
    <stroke android:width="1dp" android:color="#FF3A382F" />
</shape>
