<?xml version="1.0" encoding="utf-8"?>
<!--
  A labelled segmented control for a small enum (density, motion, haptics).

  The buttons are added in code from design.Catalog so the option list has one
  home. The detail line below the control shows the description of whichever
  option is currently selected, which is how a user learns what "Reduced"
  actually does without having to select it first.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingTop="@dimen/space_md"
    android:paddingBottom="@dimen/space_md">

    <TextView
        android:id="@+id/segmented_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textAppearance="@style/Text.SuperFlow.TitleMedium"
        android:textColor="?attr/colorOnSurface" />

    <com.google.android.material.button.MaterialButtonToggleGroup
        android:id="@+id/segmented_group"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/space_sm"
        app:singleSelection="true"
        app:selectionRequired="true" />

    <TextView
        android:id="@+id/segmented_detail"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/space_sm"
        android:textAppearance="@style/Text.SuperFlow.BodyMedium"
        android:textColor="?attr/colorOnSurfaceVariant" />
</LinearLayout>
