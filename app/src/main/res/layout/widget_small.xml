<?xml version="1.0" encoding="utf-8"?>
<!--
  2x2. Ring and a percentage, nothing else (plan 16.1).

  Only the view classes on the RemoteViews allow-list appear here; there is
  no ConstraintLayout and no custom view, because a widget is inflated in the
  launcher's process and anything else throws at inflation time.

  The ring is an ImageView fed a bitmap rather than a ProgressBar, because
  tinting a ProgressBar needs setColorStateList, which is API 31. See
  WidgetChrome for the full reasoning.
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="8dp"
    android:background="@drawable/widget_bg">

    <ImageView
        android:id="@+id/widget_ring"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:importantForAccessibility="no" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:orientation="vertical"
        android:gravity="center">

        <TextView
            android:id="@+id/widget_percent"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="20sp"
            android:textStyle="bold"
            android:includeFontPadding="false" />

        <TextView
            android:id="@+id/widget_count"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="1dp"
            android:textSize="11sp"
            android:includeFontPadding="false" />
    </LinearLayout>
</FrameLayout>
