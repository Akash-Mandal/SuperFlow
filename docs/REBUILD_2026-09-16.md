# SuperFlow — Full UI/UX Rebuild (2026-09-16)

> Working master doc for the rebuild. Every issue below was verified by
> reading the current code (file:line anchors included). Design docs dated
> before 2026-08 are historical context only.

## Ground truth of the environment
- Termux (Android, aarch64). Toolchain installed this session: Python 3.14,
  OpenJDK 17.
- **No on-device Gradle build (user decision, 2026-09-17):** the Android SDK
  (~611MB) and the Gradle 8.11.1 distribution (~130MB zip + caches) were
  installed, then deliberately purged at the user's request. Do NOT
  re-install them. Verification path is the offline static checkers
  (`tools/check_*.py`) plus code review; the JVM unit tests
  (`src/test/`) require Gradle and remain a CI/device concern.
- Offline static checkers (`tools/check_*.py`) all PASS at baseline.
- Offline design test suite lives in `tools/test/` (AndroidXShim-based).

## Verified issue inventory (by module)

### App shell (MainActivity)
1. BUG high — MainActivity.kt:222-231 shortcut check-in runs CommandBus.execute
   synchronously on the main thread (cold-start DB write; jank/ANR risk).
2. MISSING-UX med — same shortcut check-in gives no feedback/navigation.
3. BUG med — MainActivity.kt:173-183 rail insets never pads `right` (RTL break).
4. INCONS low — insets listeners registered twice; first set dead code.
5. MISSING-UX low — notification-permission denial: empty callback, no rationale.
6. GLITCH low — ViewPager2 internal RecyclerView tuning in swallowed exceptions.
7. INCONS low — double edge-to-edge setup (setDecorFitsSystemWindows + enableEdgeToEdge).
8. GLITCH low — LockActivity relaunch loop risk in onStart (no guard).

### Today (live Compose path + dormant View path)
9. BUG high — TodayViewModel refresh/build have zero exception handling; a DB
   error crashes the tab; no error state in TodayUiState.
10. BUG med — TodayFragment.kt:104-140 onMove persists reorder every drag frame.
11. BUG med — TodayViewModel.kt:384-396 reorder index computed over ALL habits,
    not the visible bucket → wrong persisted order.
12. GLITCH med — events StateFlow<String?> drops consecutive identical messages.
13. BUG high — Settings/PlanTomorrow/Recovery/Checkpoints unreachable from the
    live Compose Today (nav dead-ends).
14. GLITCH med — TextField error state never cleared on retype; persists till save.
15. DEAD low — PlanTomorrowSheet.kt unused; contains sync repo reads + LocalDate.now().
16. DEAD low — Ui.kt AppPrefs object, runEntryAnimation(), fadeIn() have no callers.
17. GLITCH low — fragment_today.xml FAB margin overridden with hardcoded 96dp.
18. INCONS low — dozens of hardcoded English strings in Today path (unlocalized).
19. BUG low — nav handlers use requireContext() → ISE if click lands post-detach.

### Journey
20. BUG high — JourneyScreen.kt:301 GapCard maps Gap.kind → wrong parent target;
    dangling-habit gap creates a SYSTEM with goalId = habitId (hierarchy
    corruption); "add habit" gap renders "Add system".
21. BUG high — JourneyFragment.kt:637-638 mirror bug in View path GapVH.
22. BUG med — ComposeJourneyFragment.kt:97-105 expanded-state projection drops
    descendants when an ancestor is collapsed.
23. BUG/MISSING med — JourneyFragment.kt:77-96 toolbar wired twice; the a11y
    refresh menu item is destroyed by menu.clear().
24. INCONS low — Compose context menu drops View menu's add/reorder entries.

### Insights
25. BUG high — InsightsFragment.kt:61,138,153 two Stats rows share stableId=2L
    with setHasStableIds(true) → stable-ID collision crash/glitch on scroll.
26. BUG med — InsightsFragment.kt:119-127 "Year" shows 90 daily bars, no weekly
    bucketing despite the comment.
27. BUG med — InsightsScreen.kt:286-292 EnergyCard claims positive tendency with
    no sign check (contradicts inverse correlation above it).
28. GLITCH low — ComposeInsightsFragment.kt:166-171 partial past days render full
    green in heatmap.
29. PERF low — ComposeInsightsFragment ~1,100 SQLite round-trips per Year refresh.
30. MISSING-UX low — no error handling in Insights ViewModels; stale rows silently.
31. A11Y low — InsightsFragment.kt:404 sub-48dp period chips; InsightsScreen.kt:430-439
    duplicated TalkBack announcements.
32. LOW — avg-consistency shows "0%" (NaN→0) with no data; SfBarChart entrance
    animation dead (animateFloatAsState initializes at target).
32. LOW — avg-consistency shows "0%" (NaN→0) with no data; SfBarChart entrance
    animation dead (animateFloatAsState initializes at target).

### Shared components / theme
33. BUG med — SfCard.kt:143-152 accent stripe `fillMaxHeight()` inside unbounded
    parent = 0 height; stripe silently disappears inside LazyColumns.
34. GLITCH med — SfCard.kt:86 isDark from isSystemInDarkTheme() ignoring SfTheme's
    explicit isDark → wrong Elevated tint on forced-dark screens.
35. MISSING-UX med — SfScreenScaffold.kt:80-84 non-list scaffold lacks nav-bar
    clearance (the list variant has it).
36. MISSING-UX med — SfCommandPalette.kt:73-76 no imePadding → keyboard covers
    results; :121-124 80ms focus race; :112-116 hardcoded "Close".
37. A11Y med — SfHeatmap.kt:104-119 day selection tap-only, no custom a11y actions.
38. INCONS low — SfTypography.kt:121-127 slot drift (titleSmall→labelLarge,
    bodySmall→labelMedium); SfPalette.kt:14 doc says five palettes, enum has seven.
39. LOW — SfTheme.kt:207 window lookup bails in dialogs; dynamic-color vs palette
    semantic extras can clash.

### Secondary screens (View layer)
40. BUG high — ActivityLogActivity.kt:62-70 buildContent re-entrancy: the load
    completion handler calls buildContent again → self-perpetuating reload loop.
41. BUG high — RoutineBuilderActivity.kt:174-184 "Start" checks in EVERY linked
    habit immediately — false check-ins corrupt history.
42. BUG med — FlowActivity.kt:45,53,124-125,142 main-thread DB reads per step;
    :153 bus.execute on main thread inside a dialog handler.
43. BUG med — BlueprintActivity.kt:50,172 plan state lost on rotation → step 3
    stays locked despite a persisted plan; :76-80 main-thread repo reads.
44. BUG med — ShareCard.kt:229-231 bitmap+PNG on main thread; :245-251 null
    stream still returns true (fake success); :33-41 palette ignored.
45. BUG med — Designer/Onboarding state loss: HabitDesignerActivity plan var and
    OnboardingScreen selections lost on rotation.
46. BUG low — PauseActivity.kt:229-240 inverted date range savable; :43,66,201
    LocalDate.now() bypasses repo.clock; :195-198 dead stubs.
47. BUG low — SearchActivity:46 main-thread LIKE search; no debounce/empty state.
48. BUG low — JournalActivity dialog shown after finish() (leak); main-thread reads.
49. MISSING-UX med — ReviewActivity missing loading state; Scorecard recomputes
    all on open; Recovery lacks undo feedback.
50. MISSING-UX low-med — ScrollActivity.kt:92-95 full-content swap on every
    interaction → scroll jumps to top.
51. INCONS low — SettingsFragment.kt:568-574 bus.execute inside nested
    TextInputSheet chains runs on the main thread (pause_habits write);
    same family at :602-615 (import).

### Studio / Settings (audited directly by Cline)
52. GLITCH med — StudioViewModel.openModelPicker (StudioFragment.kt:643-651):
    fetchModels has no try/catch — on failure modelsLoading stays true forever
    and the picker shows an eternal skeleton.
53. INCONS low — SettingsFragment.kt:72,565,597,604 hardcoded English copy.

### Priority order
P0 (crashes/corruption): #25 #20 #21 #40 #41 #9 #13 #1
P1 (visible glitches/state): #33 #34 #35 #36 #22 #26 #27 #10 #11 #14 #43 #44 #45 #52
P2 (polish/a11y/dead code): the rest.

## Phased rebuild plan
- Phase 1 — Design-system foundation: SfCard, SfScreenScaffold, SfCommandPalette,
  SfBarChart entrance, SfHeatmap a11y, typography slots, scaffold insets.
  **DONE (commit c6db37d)** except #37 (SfHeatmap a11y) and #38 typography
  slots, still open.
- Phase 2 — P0 screen fixes (crashes, corruption, loops). **DONE (this
  session):** #25 #20 #21 #40 #41 #9 (+error state/retry in TodayScreen)
  #13 (TodayActionsRow: Settings/PlanTomorrow/Recovery/Checkpoints) #1
  (shortcut check-in moved to AppBackground + toast feedback, also covers
  #2). Verified via static checkers only.
- Phase 3 — P1 glitches + missing UX states (error/empty/loading everywhere).
- Phase 4 — Navigation reachability + feedback + polish; dead code removal.
- Phase 5 — Verify: static checkers (this is now the only local gate — see
  ground truth). Commit after every verified phase; never push without
  instruction.
